Config = Config or {}

Config.blipsShow = true

-- The blips are now managed via the database and the in-game command /blipscreator
-- This table is no longer used for blip locations.
Config.Locations = {}
